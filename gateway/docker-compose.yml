services:
  gateway:
    build:
      context: .
      target: runtime
    ports:
      - "${GATEWAY_PORT:-3000}:3000"
    env_file: .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
